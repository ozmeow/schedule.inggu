spring.application.name=расписание пар

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/schedule_bot
spring.datasource.username=schedule_user
spring.datasource.password=3211

# ? CONNECTION POOL
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000

# =============================================
# ?? JPA / HIBERNATE CONFIGURATION
# =============================================
# ? DDL STRATEGY: update - ????????????? ??????? ???????
spring.sql.init.mode=always

# ? DEVELOPER SETTINGS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ? LOGGING
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# Create database if not exists (????? ??????? ???????)
# CREATE DATABASE schedule_bot;